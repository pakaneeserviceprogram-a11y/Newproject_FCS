# AGENTS.md - คู่มือควบคุม AI ให้ทำงานอย่างมีประสิทธิภาพ

ไฟล์นี้ออกแบบมาเพื่อแก้ปัญหา **"AI เอ๋อ"** หรือการที่ AI ไม่ทำตามคำสั่ง (Instruction Following) โดยใช้เทคนิค **Prompt Engineering** ระดับสูง เพื่อกำหนดกรอบการทำงานให้ AI อย่างชัดเจนและลดการมโน (Hallucination)

---

## 🎯 1. การกำหนดบทบาทและเป้าหมาย (Role & Goal)
AI จะทำงานได้ดีที่สุดเมื่อรู้ว่าตัวเองเป็นใครและต้องทำอะไร ให้ระบุสิ่งนี้ที่ส่วนบนสุดของไฟล์เสมอ:

> **Role:** คุณคือผู้เชี่ยวชาญด้าน [ระบุสาขา เช่น Full-stack Developer, Data Scientist] ที่เน้นความถูกต้องและประสิทธิภาพสูงสุด
> **Goal:** เป้าหมายของคุณคือ [ระบุเป้าหมายหลักของโปรเจกต์] โดยต้องปฏิบัติตามกฎในไฟล์นี้อย่างเคร่งครัด

---

## 🛠️ 2. กฎเหล็กเพื่อป้องกัน AI เอ๋อ (Core Rules)
ใช้โครงสร้าง **Do** และ **Don't** เพื่อให้ AI เข้าใจขอบเขตที่ชัดเจน:

### ✅ สิ่งที่ต้องทำ (Do)
- **Be Specific:** ระบุเวอร์ชันของ Library หรือ Framework ที่ใช้เสมอ (เช่น React 18, Next.js 14)
- **Chain of Thought:** สั่งให้ AI "คิดทีละขั้นตอน" (Think step-by-step) ก่อนเริ่มเขียน Code หรือคำตอบ
- **Verification:** สั่งให้ AI ตรวจสอบความถูกต้องของคำตอบตัวเองก่อนส่ง (Self-Correction)
- **Concise Output:** เน้นคำตอบที่กระชับ ตรงประเด็น และหลีกเลี่ยงการอธิบายที่ยืดเยื้อเกินจำเป็น

### ❌ สิ่งที่ไม่ควรทำ (Don't)
- **No Hallucination:** หากไม่แน่ใจในข้อมูล ให้ตอบว่า "ไม่ทราบ" แทนการคาดเดา
- **No Legacy Code:** ห้ามใช้ฟังก์ชันที่เลิกใช้แล้ว (Deprecated) หรือรูปแบบการเขียนแบบเก่า
- **No Assumptions:** ห้ามทึกทักเอาเองเกี่ยวกับโครงสร้างไฟล์หรือตัวแปร หากไม่พบในบริบทให้ถามก่อน

---

## 📋 3. โครงสร้างคำสั่งที่มีประสิทธิภาพ (Instruction Structure)
เพื่อให้ AI ไม่หลุดโฟกัส ควรใช้สัญลักษณ์แบ่งส่วนข้อมูล (Delimiters) เช่น:

- ใช้ `###` หรือ `---` เพื่อแบ่งหัวข้อหลัก
- ใช้ `[CONTEXT]`, `[INSTRUCTION]`, `[OUTPUT_FORMAT]` เพื่อระบุส่วนต่างๆ ของคำสั่ง

---

## 🚀 4. เทคนิคขั้นสูงเพื่อเพิ่มความแม่นยำ
1. **Few-Shot Prompting:** ให้ตัวอย่าง (Example) ของผลลัพธ์ที่ต้องการอย่างน้อย 1-2 ตัวอย่าง
2. **Instruction Budget:** อย่าใส่คำสั่งมากเกินไปในครั้งเดียว (แนะนำไม่เกิน 150-200 คำสั่งต่อไฟล์)
3. **Progressive Disclosure:** หากโปรเจกต์ใหญ่ ให้แยกไฟล์คำสั่งย่อยๆ (เช่น `docs/TYPESCRIPT.md`) แล้วอ้างถึงในไฟล์นี้แทน

---

## 🔍 5. ตัวอย่างการตั้งค่าสำหรับ Coding Agent
หากคุณใช้ AI ช่วยเขียนโปรแกรม ให้เพิ่มส่วนนี้:

```markdown
### Coding Standards
- Use TypeScript for all new files.
- Follow Functional Programming patterns.
- Ensure all components are accessible (WCAG 2.1).

### Commands
- Type check: `npm run tsc --noEmit`
- Lint: `npm run eslint --fix`
- Test: `npm test`
```

---

## 💡 เคล็ดลับสุดท้าย
หาก AI เริ่มไม่ฟังคำสั่ง ให้ลองใช้ประโยคนี้ใน Prompt:
> *"ทบทวนกฎใน AGENTS.md อย่างละเอียด และตรวจสอบว่าคำตอบของคุณขัดต่อกฎข้อใดหรือไม่ ก่อนที่จะส่งคำตอบ"*

