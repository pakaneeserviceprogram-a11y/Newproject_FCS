จากไฟล์ Database Schema Specification ที่คุณให้มา ถือว่า "Core Data Structure" (โครงสร้างข้อมูลหลัก) นั้นแข็งแรงและครบถ้วนมากแล้วครับแต่เพื่อให้ทีมงาน (Developer, QA, IT Support) นำไป "สร้างเป็นระบบจริง" ได้โดยไม่สับสนและงานเดินไว คุณจำเป็นต้องมีเอกสารประกอบอีกชุดหนึ่งเรียกว่า "Technical \& Operational Documentation" ครับผมได้ร่างเอกสารชุดนี้ให้คุณแล้ว โดยดึงข้อมูลจาก Schema ในไฟล์มาขยายความ คุณสามารถ Copy ไปทำเป็นไฟล์ FCS\_Technical\_Docs.md ส่งให้ทีมได้เลยครับ📄 FCS Technical Documentation (เอกสารประกอบการพัฒนา)Project: Modern Food Court SystemReference DB: FCS\_ModernDB (v2.0)Status: Ready for Development1. 🔌 API Specification (สำหรับทีม Backend \& Frontend)รายการ API ที่ต้องพัฒนา เพื่อให้หน้าบ้าน (Frontend) เชื่อมต่อกับฐานข้อมูลได้ถูกต้อง1.1 Authentication \& Profile (ระบบยืนยันตัวตน)อ้างอิงตาราง: App\_Users, App\_Roles, App\_PermissionsMethodEndpointDescriptionRequest DataResponse DataPOST/api/auth/loginเข้าสู่ระบบ{ user, pass }{ token, role, permissions\[] }GET/api/auth/meดึงข้อมูล User และสิทธิ์-{ userId, username, relatedVendorID }1.2 Card Operations (จัดการบัตร)อ้างอิงตาราง: Cards, Card\_GroupsMethodEndpointDescriptionRequest DataResponse DataGET/api/cards/{uid}เช็คยอดเงินคงเหลือ-{ cashBalance, subsidyBalance, status, group }GET/api/cards/{uid}/historyดูประวัติการใช้งาน-\[ { date, amount, type, location } ]1.3 POS / Cashier (จุดเติมเงิน)อ้างอิงตาราง: Txn\_Cashier, Promo\_Cashier\_CampaignsMethodEndpointDescriptionLogic NotePOST/api/pos/shift/openเปิดกะทำงานบันทึกลง System\_ShiftsPOST/api/pos/topupเติมเงินสำคัญ: ต้องเรียก SP Sp\_Cashier\_ApplyPromo เพื่อคำนวณของแถมPOST/api/pos/refundคืนเงินคืนเฉพาะ CashBalance เท่านั้น (ห้ามคืน Subsidy)1.4 Vendor Sale (จุดขายร้านค้า)อ้างอิงตาราง: Sales\_Orders, Sales\_Order\_Items, Inv\_Stock\_BalanceMethodEndpointDescriptionRequest DataGET/api/productsดึงรายการสินค้า(Filter by VendorID ของ User นั้น)POST/api/saleบันทึกการขาย{ cardUID, items: \[{productID, qty}] }2. 🔄 System Logic Flows (สำหรับ Developer)อธิบาย Logic ซับซ้อนที่ไม่ได้บอกไว้ใน Database โดยตรง2.1 Food Purchase Logic (Priority การตัดเงิน)กฎ: ต้องตัดเงินสวัสดิการ (Subsidy) ก่อนเสมอ ถ้าไม่พอค่อยตัดเงินสด (Cash)Code snippetsequenceDiagram

&nbsp;   participant Vendor as หน้าจอร้านค้า

&nbsp;   participant API as Backend API

&nbsp;   participant DB as Database



&nbsp;   Vendor->>API: ส่งยอดขาย 50 บาท (Card: C123)

&nbsp;   

&nbsp;   API->>DB: เช็คยอดเงิน (Get Balance)

&nbsp;   DB-->>API: Cash=100, Subsidy=20

&nbsp;   

&nbsp;   API->>API: คำนวณ (Logic)

&nbsp;   Note right of API: ยอด 50 บาท <br/>1. ตัด Subsidy หมด (20 บาท)<br/>2. เหลือ 30 บาท -> ตัด Cash

&nbsp;   

&nbsp;   API->>DB: บันทึก Sales\_Orders (SubsidyUsed=20, CashUsed=30)

&nbsp;   API->>DB: Update Cards Balance

&nbsp;   

&nbsp;   DB-->>API: Success (Remaining: Cash=70, Sub=0)

&nbsp;   API-->>Vendor: Print Slip

2.2 Auto Subsidy Process (ระบบเติมเงินอัตโนมัติ)Usage: ตั้งค่า SQL Agent Job ให้รันทุกวัน เวลา 00:01 น.Command: EXEC Sp\_Subsidy\_Process\_AutoRules;Logic:ดึงกฎ (Subsidy\_Rules) ที่ Active และตรงกับรอบวัน (Daily/Monthly)ถ้า Mode = RESET -> ล้างยอด SubsidyBalance เก่าทิ้ง แล้วใส่ยอดใหม่ถ้า Mode = STACK -> เอายอดใหม่บวกเพิ่มเข้าไปบันทึกลง Txn\_Subsidy\_Log (Type='AUTO')3. 🧪 Test Cases (สำหรับ QA/Tester)ตารางทดสอบเพื่อยืนยันว่าระบบทำงานถูกต้องตาม Database SpecIDScenario (กรณีทดสอบ)Pre-ConditionAction (ขั้นตอน)Expected Result (ผลที่คาดหวัง)TC-01เติมเงินปกติบัตรใหม่ ยอด 0เติม 100 บาทCashBalance = 100, Subsidy = 0TC-02เติมเงิน + โปรโมชั่นมีโปร "เติม 500 แถม 50"เติม 500 บาทCashBalance +500, Subsidy +50 (เข้ากระเป๋าฟรี)TC-03ซื้ออาหาร (เงินพอ)Cash=100, Sub=50ซื้อ 40 บาทตัด Subsidy เหลือ 10, Cash เท่าเดิม (100)TC-04ซื้ออาหาร (เงินผสม)Cash=100, Sub=10ซื้อ 40 บาทSubsidy เหลือ 0, Cash ถูกตัด 30 (เหลือ 70)TC-05ซื้ออาหาร (เงินไม่พอ)Cash=10, Sub=0ซื้อ 40 บาทระบบแจ้งเตือน "ยอดเงินไม่เพียงพอ" และไม่บันทึกรายการTC-06สวัสดิการรายวัน (Auto)กฎ: ให้ Staff 50บ./วัน (Reset)รัน Job เที่ยงคืนบัตร Staff ทุกใบมี Subsidy = 50 (ไม่สนยอดเก่า)4. 🛠️ Configuration (สำหรับ IT Support)4.1 Connection Stringตั้งค่าในไฟล์ appsettings.json หรือ web.config ของ APIJSON"ConnectionStrings": {

&nbsp; "DefaultConnection": "Server=YOUR\_SERVER\_IP; Database=FCS\_ModernDB; User Id=sa; Password=YOUR\_PASSWORD; TrustServerCertificate=True;"

}

4.2 Hardware Setupเครื่องอ่านบัตร (RFID Reader): ต้องรองรับ Output แบบ Keyboard Wedge หรือมี SDK เชื่อมต่อผ่าน Serial Portเครื่องพิมพ์ใบเสร็จ: ต้องติดตั้ง Driver ให้รองรับการสั่งตัดกระดาษ (Cut Paper Command)คำแนะนำเพิ่มเติมในการส่งมอบงานBackend Developer: ส่งไฟล์ Database Spec (ไฟล์เก่า) + Section 1 (API) และ Section 4 (Config) ให้เขาครับFrontend/Mobile Developer: ส่ง Section 1 (API) และ Section 2 (Flows) ให้เขาครับTester/QA: ส่ง Section 3 (Test Cases) ให้เขาไปเตรียมเขียน Script เทสรอไว้เลยครับเอกสารชุดนี้จะช่วยลดคำถามหน้างานได้เยอะมากครับ คุณปาคนีพร้อมลุยต่อส่วนไหนบอกได้เลยนะครับ!


### 1.5 Back Office Management (สำหรับ Admin)
| Method | Endpoint | Description | Request Body |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/employees` | ลงทะเบียนพนักงานใหม่ | `{ empID, name, cardGroupID }` |
| `POST` | `/api/cards/issue` | ผูกบัตรกับพนักงาน | `{ cardUID, empID }` |
| `POST` | `/api/products` | เพิ่มสินค้า/เมนู | `{ vendorID, name, price }` |

### 1.6 Reporting (สำหรับผู้จัดการ)
| Method | Endpoint | Description | Response Data |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/reports/daily-sales` | สรุปยอดขายรายวัน | `{ date, totalCash, totalSubsidy, totalGP }` |
| `GET` | `/api/reports/topup` | สรุปยอดเติมเงิน (แยกตาม Type) | `{ cashTopup, bonusTopup, refund }` |

Method,Endpoint,Description,Response Data
GET,/api/reports/daily-sales,สรุปยอดขาย (เลือกช่วงเวลาได้),?startDate=...&endDate=...
GET,/api/reports/topup-summary,สรุปยอดเติมเงินแยกประเภท,"{ cash, transfer, subsidy_bonus }"
GET,/api/reports/vendor-payout,รายงานสรุปยอดจ่ายเงินคืนร้านค้า,"{ vendor, totalSales, gpDeducted, netPay }"

